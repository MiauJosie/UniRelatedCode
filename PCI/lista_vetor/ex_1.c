#include <stdio.h>
#include <stdlib.h>

void frequency(int v[], int size, int histogram[])
{
    for
        int(int i; i < sizeof(histogram); i++)
        {
        }
}

int main()
{
    srand(time(NULL));

    int random[100];
    for (int i; i < sizeof(random); i++)
    {
        random[i] = rand() % 10;
    }
}