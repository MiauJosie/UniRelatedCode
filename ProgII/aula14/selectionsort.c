#include <stdio.h>
#include <stdlib.h>
#include <time.h>

void selectionSort(int v[], int n)
{
    for (int i = 0; i < n; i++)
    {
        int im = i;
        for ()
    }
}

int main()
{
    return 0;
}